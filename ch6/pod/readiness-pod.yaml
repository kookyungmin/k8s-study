apiVersion: v1
kind: Pod
metadata:
  name: readiness-pod
spec:
  containers:
  - name: readiness-container
    image: nginx:1.19.1
    ports:
      - containerPort: 80
    readinessProbe:
      httpGet:
        path: /testpath
        port: 80
      initialDelaySeconds: 5
      periodSeconds: 5
